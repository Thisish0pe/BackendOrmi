'''
Tuple : 셀 수 있는 수량의 순서있는 열거

t = ()
t = (1,) -> 하나만 있어도 콤마 찍어주기

1. 값이 변하지 않는다 -> immutable -> 깊은 복사
2. 시퀀스형 -> 인덱스 값 가짐 -> 인덱싱. 슬라이싱 가능 -> 인덱스 함수 사용 가능 

----

시퀀스형
종류
문자형, 리스트, 튜플

특징
1. in, not in
2. 같은 자료형끼리 연결 가능 (list1 + list2)
3. 객체 반복 가능 (list1 * 3 = [] [] [])
4. 같은 요소의 개수 확인 가능 (.count('8'))
5. 전체 길이 (len())

----


'''